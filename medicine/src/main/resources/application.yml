server:
  port: 8083

spring:
  application:
    name: medicine

  datasource:
    url: jdbc:mysql://localhost:3306/java_senepharma_medicines
    username: root
    password: dieme
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect